#!/usr/bin/env python

import numpy as np
import sys
from sklearn.preprocessing import normalize

x = np.loadtxt(sys.stdin)
x = normalize(x)
x = np.sum(x)
np.savetxt(sys.stdout, x, fmt='%d', sep='\n')
